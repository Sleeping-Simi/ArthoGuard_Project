name: arthoguard_project
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  camera: ^0.12.0+2
  image_picker: ^1.2.3
  google_sign_in: ^7.2.0
  http: ^1.6.0
  model_viewer_plus: ^1.10.0
  file_picker: ^12.0.0
  intl: ^0.20.3
  shared_preferences: ^2.5.3
dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/reference_knee.png